package JavaCLass;
/**
 * 
 *   interface does not allow the non abstract method
 *   and every method inside the interface is by default abstract by nature.
 *   
 *   so we can say that interface do have 100% abstraction bside the abstract class
 *   
 *   
 */
public interface fatherinter {
	public void inteligence();

	public void hairloss();

}
