package JavaCLass;

/**
 * 
 * if else
 * 
 * ===Defference 
 * 
 * elseif ladder
 * 
 * if (a < b) {

			System.out.println("Sum of two number is " + (a + b));

		} else if (a > b) {
			System.out.println("multipication of two number is " + (a * b));

		} else if (a == b) {
			System.out.println("devison of two number is " + (a / b));

		} else {
			System.out.println("substraction of two number is " + (a - b));
		}
 * 
 * ===Defference
 * The Java switch statement is fall-through. It means it executes all statements after the first match if a
   break statement is not present.
 * switchcase
 * int key =5;
		switch (5) {
		case 5:
			System.out.println("Sum of two number is " + (a + b));
			break;
		case 6:
			System.out.println("multipication of two number is " + (a * b));
			break;
		case 7:
			System.out.println("devison of two number is " + (a / b));
			break;
		default:
			System.out.println("substraction of two number is " + (a - b));
			break;
		}
 *
 *
		
 */
public class conditoinalStatment {

	public static void main(String[] args) {
		int a = 20;
		int b = 20;
		

	}

}
